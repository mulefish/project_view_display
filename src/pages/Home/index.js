//import { connect } from 'react-redux'
import Home from './layout' // TODO: 'Home' ought to be called layout

// const mapStateToProps = () => ({})

// const mapDispatchToProps = {

// }
// export default connect(mapStateToProps, mapDispatchToProps)(Home)
export default Home